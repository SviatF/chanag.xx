import Header from "@/components/Header";import Link from "next/link";import {cityBySlug} from "@/lib/cities";import {getMonthlyMuhurat} from "@/lib/muhurat";import {formatWindow} from "@/lib/panchang";
export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{event:string;year:string;month:string;city:string}>}){const p=await params;const city=cityBySlug(p.city);const {rule,rows}=await getMonthlyMuhurat(p.event,Number(p.year),Number(p.month),city);return <main><Header city={city}/><div className="page-shell"><p className="page-kicker">CITY-SPECIFIC MUHURAT</p><h1 className="page-title">{rule.title}<br/>{city.name}</h1><p className="page-subtitle">{rule.note}</p><div className="wide-panel"><table className="table"><thead><tr><th>Date</th><th>Tithi</th><th>Nakshatra</th><th>Abhijit</th><th>Rahu Kalam</th></tr></thead><tbody>{rows.map(r=><tr key={r.date}><td><Link href={`/panchang/${city.slug}/${r.date}`}>{r.date}</Link></td><td>{r.data.tithi}</td><td>{r.data.nakshatra}</td><td>{formatWindow(r.data.abhijit)}</td><td>{formatWindow(r.data.rahu)}</td></tr>)}</tbody></table></div></div></main>}
