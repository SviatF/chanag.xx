import type {Metadata} from "next";import Link from "next/link";import Header from "@/components/Header";import {cityBySlug} from "@/lib/cities";import {formatWindow} from "@/lib/panchang";import {getMonthlyMuhurat,muhuratRules} from "@/lib/muhurat";
export const dynamic="force-dynamic";
export async function generateMetadata({params}:{params:Promise<{event:string;year:string;month:string}>}):Promise<Metadata>{const p=await params;const rule=muhuratRules[p.event]??muhuratRules.wedding;return{title:`${rule.title} ${p.year} — Best Dates in ${p.month}`,description:`Candidate ${rule.title.toLowerCase()} dates and local auspicious windows for ${p.month}/${p.year}.`}}
export default async function MuhuratPage({params,searchParams}:{params:Promise<{event:string;year:string;month:string}>;searchParams:Promise<{city?:string}>}){const p=await params;const q=await searchParams;const city=cityBySlug(q.city??"mumbai");const {rule,rows}=await getMonthlyMuhurat(p.event,Number(p.year),Number(p.month),city);const ld={"@context":"https://schema.org","@graph":rows.map(r=>({"@type":"Event","name":`${rule.title} — ${r.date}`,"startDate":r.date,"location":{"@type":"Place","name":city.name},"description":rule.note}))};
return <main><Header city={city}/><div className="page-shell"><p className="page-kicker">AUSPICIOUS PLANNING · {city.name}</p><h1 className="page-title">{rule.title}<br/>{p.month}/{p.year}</h1><p className="page-subtitle">{rule.note} This page is a general Panchang-based shortlist rather than a personalized Kundli consultation.</p><div className="pill-links">{["mumbai","delhi","kolkata","chennai","bengaluru"].map(slug=><Link href={`?city=${slug}`} key={slug}>{cityBySlug(slug).name}</Link>)}</div><div className="wide-panel"><table className="table"><thead><tr><th>Date</th><th>Tithi</th><th>Nakshatra</th><th>Abhijit</th><th>Avoid Rahu</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.date}><td><Link href={`/panchang/${city.slug}/${r.date}`}>{r.date}</Link></td><td>{r.data.tithi}</td><td>{r.data.nakshatra}</td><td>{formatWindow(r.data.abhijit)}</td><td>{formatWindow(r.data.rahu)}</td></tr>):<tr><td colSpan={5}>No candidates matched the current rule set for this month.</td></tr>}</tbody></table></div><div className="seo-copy"><h2>How these dates are selected</h2><p>{rule.note} The calculation remains location-sensitive because sunrise-based periods such as Rahu Kalam vary by city. Personal horoscope compatibility and specialized ritual constraints are intentionally outside this general-purpose shortlist.</p></div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(ld)}}/></div></main>}
