import Header from "@/components/Header";import {cityBySlug,cities} from "@/lib/cities";import {getPanchang} from "@/lib/panchang";
export const dynamic="force-dynamic";export const metadata={title:"Nakshatra Finder",description:"Find birth Nakshatra and Pada from date and location."};
export default async function Page({searchParams}:{searchParams:Promise<{city?:string;date?:string}>}){const q=await searchParams;const city=cityBySlug(q.city??"mumbai");const raw=q.date&&/^\d{4}-\d{2}-\d{2}$/.test(q.date)?q.date:"1995-01-01";const data=await getPanchang(new Date(raw+"T06:00:00Z"),city);return <main><Header city={city}/><div className="page-shell internal-visual internal-tools"><p className="page-kicker">BIRTH TOOL</p><h1 className="page-title">Nakshatra Finder</h1><form className="wide-panel" method="get"><label>City<br/><select name="city" defaultValue={city.slug}>{cities.map(c=><option value={c.slug} key={c.slug}>{c.name}</option>)}</select></label><label>Date<br/><input type="date" name="date" defaultValue={raw}/></label><button className="gold-button" type="submit">Find Nakshatra →</button></form><div className="data-grid"><div className="data-card"><small>Nakshatra</small><strong>{data.nakshatra}</strong></div><div className="data-card"><small>Pada</small><strong>{data.nakshatraPada}</strong></div><div className="data-card"><small>Moon Sign</small><strong>{data.rashi}</strong></div></div></div></main>}
