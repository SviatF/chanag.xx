import Header from "@/components/Header";import {cityBySlug,cities} from "@/lib/cities";import {getPanchang} from "@/lib/panchang";
export const dynamic="force-dynamic";export const metadata={title:"Moon Sign Calculator",description:"Find your Chandra Rashi from birth date and location."};
export default async function Page({searchParams}:{searchParams:Promise<{city?:string;date?:string}>}){const q=await searchParams;const city=cityBySlug(q.city??"mumbai");const raw=q.date&&/^\d{4}-\d{2}-\d{2}$/.test(q.date)?q.date:"1995-01-01";const data=await getPanchang(new Date(raw+"T06:00:00Z"),city);const ld={"@context":"https://schema.org","@type":"WebApplication","name":"Moon Sign Calculator","applicationCategory":"LifestyleApplication","operatingSystem":"Web"};return <main><Header city={city}/><div className="page-shell internal-visual internal-tools"><p className="page-kicker">BIRTH TOOL</p><h1 className="page-title">Moon Sign Calculator</h1><p className="page-subtitle">Enter birth date and city to estimate the sidereal Chandra Rashi using the Panchang calculation layer.</p><form className="wide-panel" method="get"><label>Birth city<br/><select name="city" defaultValue={city.slug}>{cities.map(c=><option value={c.slug} key={c.slug}>{c.name}</option>)}</select></label><label>Birth date<br/><input type="date" name="date" defaultValue={raw}/></label><button className="gold-button" type="submit">Find Moon Sign →</button></form><div className="data-grid"><div className="data-card"><small>Chandra Rashi</small><strong>{data.rashi}</strong></div><div className="data-card"><small>Nakshatra</small><strong>{data.nakshatra}</strong></div><div className="data-card"><small>Pada</small><strong>{data.nakshatraPada}</strong></div></div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(ld)}}/></div></main>}
