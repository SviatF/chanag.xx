import Header from "@/components/Header";import {cityBySlug,cities} from "@/lib/cities";import {formatWindow,getPanchang} from "@/lib/panchang";import {todayInIndia} from "@/lib/dates";
export const dynamic="force-dynamic";export const metadata={title:"Rahu Kalam Calculator",description:"Calculate Rahu Kalam for any supported Indian city and date."};
export default async function Page({searchParams}:{searchParams:Promise<{city?:string;date?:string}>}){const q=await searchParams;const city=cityBySlug(q.city??"mumbai");const raw=q.date&&/^\d{4}-\d{2}-\d{2}$/.test(q.date)?q.date:todayInIndia().toISOString().slice(0,10);const data=await getPanchang(new Date(raw+"T06:00:00Z"),city);const ld={"@context":"https://schema.org","@type":"WebApplication","name":"Rahu Kalam Calculator","applicationCategory":"LifestyleApplication","operatingSystem":"Web"};return <main><Header city={city}/><div className="page-shell"><p className="page-kicker">CALCULATOR</p><h1 className="page-title">Rahu Kalam Calculator</h1><p className="page-subtitle">Rahu Kalam is calculated from local sunrise and sunset, so city and date both matter.</p><form className="wide-panel" method="get"><label>City<br/><select name="city" defaultValue={city.slug}>{cities.map(c=><option value={c.slug} key={c.slug}>{c.name}, {c.state}</option>)}</select></label><label>Date<br/><input type="date" name="date" defaultValue={raw}/></label><button className="gold-button" type="submit">Calculate →</button></form><div className="data-grid"><div className="data-card"><small>Rahu Kalam</small><strong>{formatWindow(data.rahu)}</strong></div><div className="data-card"><small>Sunrise</small><strong>{data.sunrise}</strong></div><div className="data-card"><small>Sunset</small><strong>{data.sunset}</strong></div><div className="data-card"><small>City</small><strong>{city.name}</strong></div></div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(ld)}}/></div></main>}
