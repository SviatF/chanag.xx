import type {Metadata} from "next";import Header from "@/components/Header";import {cities} from "@/lib/cities";import {festivals2026} from "@/lib/festivals";
export async function generateMetadata({params}:{params:Promise<{festival:string;year:string}>}):Promise<Metadata>{const p=await params;const f=festivals2026.find(x=>x.slug===p.festival);return{title:`${f?.name??p.festival} ${p.year} — Date & Panchang`,description:f?.short??"Festival date, meaning and local Panchang details."}}
export default async function FestivalPage({params}:{params:Promise<{festival:string;year:string}>}){const p=await params;const f=festivals2026.find(x=>x.slug===p.festival);const name=f?.name??p.festival.replaceAll("-"," ");const date=f?.date??`${p.year}-01-01`;const ld={"@context":"https://schema.org","@type":"Event","name":`${name} ${p.year}`,"startDate":date,"location":{"@type":"Country","name":"India"}};
return <main><Header city={cities[0]}/><div className="page-shell"><p className="page-kicker">HINDU FESTIVAL</p><h1 className="page-title">{name}<br/>{p.year}</h1><div className="data-grid"><div className="data-card"><small>Date</small><strong>{date}</strong></div><div className="data-card"><small>Region</small><strong>India</strong></div></div><div className="seo-copy"><h2>About {name}</h2><p>{f?.short??"Festival details are maintained in the festival database and can be extended with regional variations."}</p><p>Festival timing can depend on lunar-day boundaries and local tradition. City-specific variants can be layered under the same festival entity where the observance differs regionally.</p></div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(ld)}}/></div></main>}
